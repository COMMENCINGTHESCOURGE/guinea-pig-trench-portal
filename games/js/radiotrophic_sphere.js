const cv=document.getElementById('fsc');const G=cv.getContext('2d');const W=520,H=520,R=240,CX=260,CY=260;const qMul=(a,b)=>[a[0]*b[0]-a[1]*b[1]-a[2]*b[2]-a[3]*b[3],a[0]*b[1]+a[1]*b[0]+a[2]*b[3]-a[3]*b[2],a[0]*b[2]-a[1]*b[3]+a[2]*b[0]+a[3]*b[1],a[0]*b[3]+a[1]*b[2]-a[2]*b[1]+a[3]*b[0]];const qNrm=q=>{const m=Math.hypot(q[0],q[1],q[2],q[3]);return[q[0]/m,q[1]/m,q[2]/m,q[3]/m];};const qAx=(ax,ay,az,a)=>{const s=Math.sin(a/2),c=Math.cos(a/2);return[c,ax*s,ay*s,az*s];};const qRV=(v,q)=>{const[w,qx,qy,qz]=q,[vx,vy,vz]=v;const cx=qy*vz-qz*vy,cy=qz*vx-qx*vz,cz=qx*vy-qy*vx;return[vx+2*(w*cx+qy*cz-qz*cy),vy+2*(w*cy+qz*cx-qx*cz),vz+2*(w*cz+qx*cy-qy*cx)];};const vMag=v=>Math.hypot(v[0],v[1],v[2]);const vNrm=v=>{const m=vMag(v)||1;return[v[0]/m,v[1]/m,v[2]/m];};const vAdd=(a,b)=>[a[0]+b[0],a[1]+b[1],a[2]+b[2]];const vSub=(a,b)=>[a[0]-b[0],a[1]-b[1],a[2]-b[2]];const vLerp=(a,b,t)=>[a[0]+(b[0]-a[0])*t,a[1]+(b[1]-a[1])*t,a[2]+(b[2]-a[2])*t];function lcg(s){return()=>{s=(s*1664525+1013904223)>>>0;return s/4294967295;};}const rA=lcg(31415),rB=lcg(27182),rC=lcg(14142),rD=lcg(57721);const vRnd=()=>vNrm([rA()-0.5,rA()-0.5,rA()-0.5]);function prj(nx,ny,nz,zm){const a=Math.acos(Math.max(-1,Math.min(1,nz)));const rr=a/Math.PI*R*zm;const p=Math.hypot(nx,ny);if(p<1e-5)return[CX,CY];return[CX+nx/p*rr,CY+ny/p*rr];}function ic(sx,sy){return(sx-CX)**2+(sy-CY)**2<R*R*1.6;}let Q=qNrm(qAx(0.4,0.7,0.1,0.6));let ZM=1.0,AUTO=true,DRG=false,DB=0,LX=0,LY=0,FR=0;const F_R_OUTER=0.95,F_R_INNER=0.09,F_SPEED=0.006,F_STEER=0.065,F_NOISE=0.32;const SEA_Y=0.0;const GDIM=56;const densIn=new Float32Array(GDIM*GDIM);const densOut=new Float32Array(GDIM*GDIM);function stampDens(sx,sy,dir){const gx=Math.floor((sx-CX+R)/(R*2)*GDIM);const gy=Math.floor((sy-CY+R)/(R*2)*GDIM);for(let dy=-1;dy<=1;dy++)for(let dx=-1;dx<=1;dx++){const nx=gx+dx,ny=gy+dy;if(nx<0||nx>=GDIM||ny<0||ny>=GDIM)continue;const f=1-Math.abs(dx)*0.35-Math.abs(dy)*0.35;const i=ny*GDIM+nx;if(dir==='in')densIn[i]=Math.min(1,densIn[i]+0.012*f);else densOut[i]=Math.min(1,densOut[i]+0.012*f);}}function catmullSample(pts,steps){const out=[];if(pts.length<2)return pts.slice();for(let i=0;i<pts.length-1;i++){const p0=pts[Math.max(0,i-1)],p1=pts[i],p2=pts[i+1],p3=pts[Math.min(pts.length-1,i+2)];for(let s=0;s<=steps;s++){const t=s/steps,t2=t*t,t3=t2*t;out.push([0.5*((2*p1[0])+(-p0[0]+p2[0])*t+(2*p0[0]-5*p1[0]+4*p2[0]-p3[0])*t2+(-p0[0]+3*p1[0]-3*p2[0]+p3[0])*t3),0.5*((2*p1[1])+(-p0[1]+p2[1])*t+(2*p0[1]-5*p1[1]+4*p2[1]-p3[1])*t2+(-p0[1]+3*p1[1]-3*p2[1]+p3[1])*t3),0.5*((2*p1[2])+(-p0[2]+p2[2])*t+(2*p0[2]-5*p1[2]+4*p2[2]-p3[2])*t2+(-p0[2]+3*p1[2]-3*p2[2]+p3[2])*t3)]);}}return out;}let inwardTips=[],outwardTips=[],contracts=[];let permanentSplines=[];let contractCount=0;function spawnIn(){const pos=vNrm([rA()-0.5,rA()-0.5,rA()-0.5]);inwardTips.push({p:[pos[0]*F_R_OUTER,pos[1]*F_R_OUTER,pos[2]*F_R_OUTER],v:vNrm([-pos[0],-pos[1],-pos[2]]),trail:[[pos[0]*F_R_OUTER,pos[1]*F_R_OUTER,pos[2]*F_R_OUTER]],active:true});}function spawnOut(ox,oy,oz){const pos=ox!==undefined?vNrm([ox,oy,oz]):vNrm([rA()-0.5,rA()-0.5,rA()-0.5]);const r=ox!==undefined?0.06:F_R_INNER;outwardTips.push({p:[pos[0]*r,pos[1]*r,pos[2]*r],v:pos,trail:[[pos[0]*r,pos[1]*r,pos[2]*r]],active:true});}function updateFungi(){if(rA()<0.14)spawnIn();if(rA()<0.14)spawnOut();for(const tip of inwardTips){if(!tip.active)continue;const mag=vMag(tip.p);let targetDir;if(mag<0.45){const scale=7.0;const b=0.19;const tx=Math.sin(tip.p[1]*scale)-b*(tip.p[0]*scale);const ty=Math.sin(tip.p[2]*scale)-b*(tip.p[1]*scale);const tz=Math.sin(tip.p[0]*scale)-b*(tip.p[2]*scale);const attractorDir=vNrm([tx,ty,tz]);const straightDir=vNrm([-tip.p[0],-tip.p[1],-tip.p[2]]);const blend=Math.max(0,Math.min(1,(0.45-mag)/0.35));targetDir=vNrm(vLerp(straightDir,attractorDir,blend));}else{targetDir=vNrm([-tip.p[0],-tip.p[1],-tip.p[2]]);}let d=vLerp(tip.v,targetDir,F_STEER);d=vNrm(vAdd(d,[vRnd()[0]*F_NOISE,vRnd()[1]*F_NOISE,vRnd()[2]*F_NOISE]));let nx=tip.p[0]+d[0]*F_SPEED;let ny=tip.p[1]+d[1]*F_SPEED;let nz=tip.p[2]+d[2]*F_SPEED;const sld=Math.abs(ny-SEA_Y);if(sld<0.08){const damp=0.45+sld/0.08*0.5;nx=tip.p[0]+d[0]*F_SPEED*damp;nz=tip.p[2]+d[2]*F_SPEED*damp;ny=tip.p[1]+d[1]*F_SPEED*0.15;}tip.p=[nx,ny,nz];tip.v=d;tip.trail.push([nx,ny,nz]);if(tip.trail.length>80)tip.trail.shift();if(vMag(tip.p)<F_R_INNER){permanentSplines.push({pts:catmullSample(tip.trail,3),dir:'in',melanin:1});tip.active=false;if(rA()<0.5)spawnOut(tip.p[0],tip.p[1],tip.p[2]);}}for(const tip of outwardTips){if(!tip.active)continue;const targetDir=vNrm(tip.p);let d=vLerp(tip.v,targetDir,F_STEER);d=vNrm(vAdd(d,[vRnd()[0]*F_NOISE,vRnd()[1]*F_NOISE,vRnd()[2]*F_NOISE]));let nx=tip.p[0]+d[0]*F_SPEED;let ny=tip.p[1]+d[1]*F_SPEED;let nz=tip.p[2]+d[2]*F_SPEED;const sld=Math.abs(ny-SEA_Y);if(sld<0.08){const damp=0.45+sld/0.08*0.5;nx=tip.p[0]+d[0]*F_SPEED*damp;nz=tip.p[2]+d[2]*F_SPEED*damp;ny=tip.p[1]+d[1]*F_SPEED*0.15;}tip.p=[nx,ny,nz];tip.v=d;tip.trail.push([nx,ny,nz]);if(tip.trail.length>80)tip.trail.shift();if(vMag(tip.p)>F_R_OUTER){permanentSplines.push({pts:catmullSample(tip.trail,3),dir:'out',melanin:1});tip.active=false;if(rA()<0.45)spawnIn();}}for(let i=0;i<inwardTips.length;i++){if(!inwardTips[i].active)continue;for(let j=0;j<outwardTips.length;j++){if(!outwardTips[j].active)continue;if(vMag(vSub(inwardTips[i].p,outwardTips[j].p))<0.05){const mp=vLerp(inwardTips[i].p,outwardTips[j].p,0.5);contracts.push({p:mp,age:0});permanentSplines.push({pts:catmullSample(inwardTips[i].trail,3),dir:'in',melanin:1});permanentSplines.push({pts:catmullSample(outwardTips[j].trail,3),dir:'out',melanin:1});inwardTips[i].active=false;outwardTips[j].active=false;contractCount++;document.getElementById('fscon').textContent=contractCount;break;}}}for(const c of contracts)c.age++;inwardTips=inwardTips.filter(t=>t.active);outwardTips=outwardTips.filter(t=>t.active);if(permanentSplines.length>280)permanentSplines.splice(0,8);}const NS=2400;const SDX=new Float32Array(NS),SDY=new Float32Array(NS),SDZ=new Float32Array(NS);const SBR=new Float32Array(NS),SSZ=new Float32Array(NS);const SCR=new Uint8Array(NS),SCG=new Uint8Array(NS),SCB=new Uint8Array(NS);for(let i=0;i<NS;i++){const t=rA()*Math.PI*2,p=Math.acos(2*rA()-1);SDX[i]=Math.sin(p)*Math.cos(t);SDY[i]=Math.sin(p)*Math.sin(t);SDZ[i]=Math.cos(p);SBR[i]=Math.pow(rA(),1.9);SSZ[i]=0.4+rA()*1.6;const c=rA();if(c<0.07){SCR[i]=155;SCG[i]=175;SCB[i]=255;}else if(c<0.18){SCR[i]=255;SCG[i]=215;SCB[i]=165;}else if(c<0.26){SCR[i]=255;SCG[i]=145;SCB[i]=125;}else{SCR[i]=248;SCG[i]=248;SCB[i]=255;}}const NMW=640;const MWDX=new Float32Array(NMW),MWDY=new Float32Array(NMW),MWDZ=new Float32Array(NMW);const MWBR=new Float32Array(NMW),MWSZ=new Float32Array(NMW);const mn=[0.5,0.7,0.5],mnl=Math.hypot(...mn);mn[0]/=mnl;mn[1]/=mnl;mn[2]/=mnl;const mu=[-mn[1],mn[0],0],mul2=Math.hypot(...mu);mu[0]/=mul2;mu[1]/=mul2;const mv=[mn[1]*mu[2]-mn[2]*mu[1],mn[2]*mu[0]-mn[0]*mu[2],mn[0]*mu[1]-mn[1]*mu[0]];for(let i=0;i<NMW;i++){const a=rB()*Math.PI*2,sp=(rB()-0.5)*0.26;const ca=Math.cos(a),sa=Math.sin(a);let x=ca*mu[0]+sa*mv[0]+sp*mn[0],y=ca*mu[1]+sa*mv[1]+sp*mn[1],z=ca*mu[2]+sa*mv[2]+sp*mn[2];const l=Math.hypot(x,y,z);MWDX[i]=x/l;MWDY[i]=y/l;MWDZ[i]=z/l;MWBR[i]=0.06+rB()*0.22;MWSZ[i]=0.4+rB()*0.9;}const NCOLS=[[80,120,255],[255,80,110],[100,220,178],[200,90,255],[255,150,60],[120,255,150],[55,178,255],[255,200,100],[180,80,255],[100,255,200],[255,110,80],[80,200,255],[200,255,120],[255,160,80]];const NEBS=NCOLS.map(col=>{const t=rC()*Math.PI*2,p=Math.acos(2*rC()-1);return{dx:Math.sin(p)*Math.cos(t),dy:Math.sin(p)*Math.sin(t),dz:Math.cos(p),col,sz:22+rC()*80,al:0.025+rC()*0.065};});const GALS=Array.from({length:9},()=>{const t=rD()*Math.PI*2,p=Math.acos(2*rD()-1);return{dx:Math.sin(p)*Math.cos(t),dy:Math.sin(p)*Math.sin(t),dz:Math.cos(p),sz:5+rD()*13,al:0.1+rD()*0.26};});const PDR=(v=>{const l=Math.hypot(...v);return v.map(x=>x/l);})([0.55,-0.15,0.82]);function drawSplineWorld(pts,colorFn,baseAlpha,baseW){if(pts.length<2)return;for(let i=1;i<pts.length;i++){const[r1x,r1y,r1z]=qRV(pts[i-1],Q);const[r2x,r2y,r2z]=qRV(pts[i],Q);const[s1x,s1y]=prj(r1x,r1y,r1z,ZM);const[s2x,s2y]=prj(r2x,r2y,r2z,ZM);if(!ic(s1x,s1y)&&!ic(s2x,s2y))continue;const prog=i/pts.length;G.beginPath();G.moveTo(s1x,s1y);G.lineTo(s2x,s2y);G.strokeStyle=colorFn(prog,baseAlpha+prog*0.28);G.lineWidth=(baseW+prog*0.4)*ZM;G.stroke();if(FR%6===0){const mid=pts[Math.floor(pts.length/2)];const[mx,my,mz]=qRV(mid,Q);const[smx,smy]=prj(mx,my,mz,ZM);if(ic(smx,smy))stampDens(smx,smy,prog<0.5?'in':'out');}}}function renderDensity(){const cw=(R*2)/GDIM,ch=(R*2)/GDIM;const ox=CX-R,oy=CY-R;for(let gy=0;gy<GDIM;gy++)for(let gx=0;gx<GDIM;gx++){const i=gy*GDIM+gx;const dI=densIn[i],dO=densOut[i];const d=Math.max(dI,dO);if(d<0.02)continue;const sx=ox+gx*cw+cw/2,sy=oy+gy*ch+ch/2;if(!ic(sx,sy))continue;const isIntersect=dI>0.12&&dO>0.12;let r2,g2,b2;if(isIntersect){r2=200;g2=190;b2=50;}else if(dI>dO){r2=200;g2=120;b2=0;}else{r2=0;g2=200;b2=210;}G.fillStyle=`rgba(${r2},${g2},${b2},${Math.min(0.28,d*0.32)})`;G.beginPath();G.arc(sx,sy,cw*0.68,0,Math.PI*2);G.fill();}}function drawSeaLevel(){const SEA_R=Math.sqrt(Math.max(0,F_R_OUTER*F_R_OUTER-SEA_Y*SEA_Y))*0.98;G.beginPath();let first=true;for(let a=0;a<=Math.PI*2+0.01;a+=0.1){const wx=Math.cos(a)*SEA_R,wy=SEA_Y,wz=Math.sin(a)*SEA_R;const[rx,ry,rz]=qRV([wx,wy,wz],Q);const[sx,sy]=prj(rx,ry,rz,ZM);if(first){G.moveTo(sx,sy);first=false;}else G.lineTo(sx,sy);}G.closePath();G.fillStyle='rgba(15,40,70,0.14)';G.fill();G.strokeStyle='rgba(40,100,180,0.22)';G.lineWidth=0.7;G.stroke();}function render(){const time=performance.now()/1000;const bps=140/60;const beatTime=time*bps;const beatProg=beatTime%1;const barProg=(beatTime/4)%1;const transient=Math.exp(-beatProg*6);const swell=Math.sin(barProg*Math.PI);G.fillStyle='#000308';G.beginPath();G.arc(CX,CY,R,0,Math.PI*2);G.fill();G.save();G.beginPath();G.arc(CX,CY,R,0,Math.PI*2);G.clip();for(const n of NEBS){const[rx,ry,rz]=qRV([n.dx,n.dy,n.dz],Q);const[sx,sy]=prj(rx,ry,rz,ZM);if(!ic(sx,sy))continue;const fd=Math.max(0,(rz+1)*0.5);const[nr,ng,nb]=n.col,sr=n.sz*(0.5+0.5*fd);const gg=G.createRadialGradient(sx,sy,0,sx,sy,sr);gg.addColorStop(0,`rgba(${nr},${ng},${nb},${n.al*fd*3})`);gg.addColorStop(0.55,`rgba(${nr},${ng},${nb},${n.al*fd})`);gg.addColorStop(1,'rgba(0,0,0,0)');G.fillStyle=gg;G.beginPath();G.arc(sx,sy,sr,0,Math.PI*2);G.fill();}for(const g of GALS){const[rx,ry,rz]=qRV([g.dx,g.dy,g.dz],Q);const[sx,sy]=prj(rx,ry,rz,ZM);if(!ic(sx,sy))continue;G.save();G.translate(sx,sy);G.scale(1,0.36);G.fillStyle=`rgba(220,215,240,${g.al})`;G.beginPath();G.arc(0,0,g.sz,0,Math.PI*2);G.fill();G.restore();}for(let i=0;i<NMW;i++){const[rx,ry,rz]=qRV([MWDX[i],MWDY[i],MWDZ[i]],Q);const[sx,sy]=prj(rx,ry,rz,ZM);if(!ic(sx,sy))continue;G.fillStyle=`rgba(215,205,188,${MWBR[i]*0.65})`;G.beginPath();G.arc(sx,sy,MWSZ[i],0,Math.PI*2);G.fill();}for(let i=0;i<NS;i++){const[rx,ry,rz]=qRV([SDX[i],SDY[i],SDZ[i]],Q);const[sx,sy]=prj(rx,ry,rz,ZM);if(!ic(sx,sy))continue;const dr=Math.hypot(sx-CX,sy-CY)/R;const ef=dr>0.78?Math.pow(Math.max(0,1-(dr-0.78)/0.22),1.4):1;G.fillStyle=`rgba(${SCR[i]},${SCG[i]},${SCB[i]},${SBR[i]*ef})`;G.beginPath();G.arc(sx,sy,SSZ[i],0,Math.PI*2);G.fill();}const[prx,pry,prz]=qRV(PDR,Q);const[ppx,ppy]=prj(prx,pry,prz,ZM);if(ic(ppx,ppy)){const pR=13;const pg=G.createRadialGradient(ppx,ppy,0,ppx,ppy,pR*3.2);pg.addColorStop(0,'rgba(80,160,220,0.2)');pg.addColorStop(1,'rgba(0,0,0,0)');G.fillStyle=pg;G.beginPath();G.arc(ppx,ppy,pR*3.2,0,Math.PI*2);G.fill();const pb=G.createRadialGradient(ppx-pR*0.35,ppy-pR*0.35,1,ppx,ppy,pR);pb.addColorStop(0,'#4d8a5d');pb.addColorStop(0.55,'#1a5590');pb.addColorStop(1,'#071525');G.fillStyle=pb;G.beginPath();G.arc(ppx,ppy,pR,0,Math.PI*2);G.fill();G.beginPath();G.arc(ppx,ppy,pR+2.5,0,Math.PI*2);G.strokeStyle='rgba(80,160,220,0.42)';G.lineWidth=2;G.stroke();}renderDensity();drawSeaLevel();G.lineCap='round';for(const s of permanentSplines){const colFn=s.dir==='in'?(prog,al)=>`rgba(255,${Math.round(140+prog*30)},0,${al})`:(prog,al)=>`rgba(0,${Math.round(200+prog*55)},${Math.round(200+prog*55)},${al})`;drawSplineWorld(s.pts,colFn,0.12,0.45);}for(const tip of inwardTips){if(tip.trail.length<3)continue;const spl=catmullSample(tip.trail.slice(-30),3);for(let i=1;i<spl.length;i++){const[r1x,r1y,r1z]=qRV(spl[i-1],Q);const[r2x,r2y,r2z]=qRV(spl[i],Q);const[s1x,s1y]=prj(r1x,r1y,r1z,ZM);const[s2x,s2y]=prj(r2x,r2y,r2z,ZM);if(!ic(s1x,s1y)&&!ic(s2x,s2y))continue;const mag=vMag(spl[i]);G.beginPath();G.moveTo(s1x,s1y);G.lineTo(s2x,s2y);G.strokeStyle=`rgba(255,170,0,${0.07+(1-mag)*0.6})`;G.lineWidth=1.1*ZM;G.stroke();}const[trx,try2,trz]=qRV(tip.p,Q);const[tsx,tsy]=prj(trx,try2,trz,ZM);if(ic(tsx,tsy)){G.beginPath();G.arc(tsx,tsy,1.8*ZM,0,Math.PI*2);G.fillStyle='rgba(255,200,80,0.85)';G.fill();}}for(const tip of outwardTips){if(tip.trail.length<3)continue;const spl=catmullSample(tip.trail.slice(-30),3);for(let i=1;i<spl.length;i++){const[r1x,r1y,r1z]=qRV(spl[i-1],Q);const[r2x,r2y,r2z]=qRV(spl[i],Q);const[s1x,s1y]=prj(r1x,r1y,r1z,ZM);const[s2x,s2y]=prj(r2x,r2y,r2z,ZM);if(!ic(s1x,s1y)&&!ic(s2x,s2y))continue;const mag=vMag(spl[i]);G.beginPath();G.moveTo(s1x,s1y);G.lineTo(s2x,s2y);G.strokeStyle=`rgba(0,255,255,${0.07+mag*0.6})`;G.lineWidth=1.1*ZM;G.stroke();}const[trx,try3,trz]=qRV(tip.p,Q);const[tsx,tsy]=prj(trx,try3,trz,ZM);if(ic(tsx,tsy)){G.beginPath();G.arc(tsx,tsy,1.8*ZM,0,Math.PI*2);G.fillStyle='rgba(0,255,220,0.85)';G.fill();}}for(const c of contracts){const fade=Math.max(0.3,1-c.age/300);const[rx,ry,rz]=qRV(c.p,Q);const[sx,sy]=prj(rx,ry,rz,ZM);if(!ic(sx,sy))continue;const nodePulse=2.5+(transient*1.5);G.beginPath();G.arc(sx,sy,nodePulse*ZM,0,Math.PI*2);G.fillStyle=`rgba(255,215,0,${0.9*fade})`;G.fill();}const[cpx2,cpy2]=prj(0,0,0,ZM);const glow=15+(transient*7)+(swell*4);const cg1=G.createRadialGradient(cpx2,cpy2,1,cpx2,cpy2,glow);cg1.addColorStop(0,'rgba(255,160,40,0.82)');cg1.addColorStop(0.35,'rgba(200,80,10,0.38)');cg1.addColorStop(0.7,'rgba(120,30,5,0.08)');cg1.addColorStop(1,'rgba(0,0,0,0)');G.beginPath();G.arc(cpx2,cpy2,glow,0,Math.PI*2);G.fillStyle=cg1;G.fill();const cg2=G.createRadialGradient(cpx2,cpy2,0,cpx2,cpy2,glow*0.5);cg2.addColorStop(0,'rgba(0,240,175,0.55)');cg2.addColorStop(0.5,'rgba(0,155,120,0.16)');cg2.addColorStop(1,'rgba(0,0,0,0)');G.beginPath();G.arc(cpx2,cpy2,glow*0.5,0,Math.PI*2);G.fillStyle=cg2;G.fill();G.beginPath();G.arc(cpx2,cpy2,2.3,0,Math.PI*2);G.fillStyle='rgba(240,240,200,0.95)';G.fill();G.restore();const r1=G.createRadialGradient(CX,CY,R-14,CX,CY,R+2);r1.addColorStop(0,'rgba(0,0,0,0)');r1.addColorStop(0.65,'rgba(0,0,0,0.28)');r1.addColorStop(1,'rgba(0,0,0,0.93)');G.beginPath();G.arc(CX,CY,R+2,0,Math.PI*2);G.fillStyle=r1;G.fill();const r2=G.createRadialGradient(CX-88,CY-88,4,CX-52,CY-52,R*0.74);r2.addColorStop(0,'rgba(255,255,255,0.12)');r2.addColorStop(0.35,'rgba(255,255,255,0.025)');r2.addColorStop(1,'rgba(255,255,255,0)');G.beginPath();G.arc(CX,CY,R,0,Math.PI*2);G.fillStyle=r2;G.fill();const r3=G.createRadialGradient(CX,CY,R-1,CX,CY,R+16);r3.addColorStop(0,'rgba(68,110,200,0.08)');r3.addColorStop(0.5,'rgba(68,110,200,0.03)');r3.addColorStop(1,'rgba(0,0,0,0)');G.beginPath();G.arc(CX,CY,R+16,0,Math.PI*2);G.fillStyle=r3;G.fill();G.beginPath();G.arc(CX,CY,R,0,Math.PI*2);G.strokeStyle='rgba(100,145,225,0.18)';G.lineWidth=1.5;G.stroke();if(FR%30===0){const tot=densIn.filter(v=>v>0.05).length+densOut.filter(v=>v>0.05).length;document.getElementById('fsvol').textContent=Math.round(tot/(GDIM*GDIM)*100);}}cv.addEventListener('mousedown',e=>{DRG=true;DB=e.button;LX=e.clientX;LY=e.clientY;AUTO=false;document.getElementById('fsmode').textContent='\u25CB exploring';e.preventDefault();});window.addEventListener('mouseup',()=>{DRG=false;});window.addEventListener('mousemove',e=>{if(!DRG)return;const dx=(e.clientX-LX)/R,dy=(e.clientY-LY)/R;LX=e.clientX;LY=e.clientY;if(DB===2)Q=qNrm(qMul(qAx(0,0,1,dx*2.2),Q));else Q=qNrm(qMul(qAx(0,1,0,-dx*2),qMul(qAx(1,0,0,dy*2),Q)));});cv.addEventListener('contextmenu',e=>e.preventDefault());cv.addEventListener('wheel',e=>{ZM=Math.max(0.35,Math.min(5,ZM*(e.deltaY>0?0.92:1.087)));document.getElementById('fszm').textContent=ZM.toFixed(2);e.preventDefault();},{passive:false});window.addEventListener('keydown',e=>{const sp=0.045;if(e.key==='ArrowLeft')Q=qNrm(qMul(qAx(0,1,0,sp),Q));if(e.key==='ArrowRight')Q=qNrm(qMul(qAx(0,1,0,-sp),Q));if(e.key==='ArrowUp')Q=qNrm(qMul(qAx(1,0,0,-sp),Q));if(e.key==='ArrowDown')Q=qNrm(qMul(qAx(1,0,0,sp),Q));if(e.key==='q'||e.key==='Q')Q=qNrm(qMul(qAx(0,0,1,-sp),Q));if(e.key==='e'||e.key==='E')Q=qNrm(qMul(qAx(0,0,1,sp),Q));if(e.key==='+'||e.key==='=')ZM=Math.min(5,ZM*1.1);if(e.key==='-')ZM=Math.max(0.35,ZM/1.1);AUTO=false;document.getElementById('fsmode').textContent='\u25CB exploring';document.getElementById('fszm').textContent=ZM.toFixed(2);});let TP={};cv.addEventListener('touchstart',e=>{e.preventDefault();AUTO=false;document.getElementById('fsmode').textContent='\u25CB exploring';for(let t of e.changedTouches)TP[t.identifier]={x:t.clientX,y:t.clientY};},{passive:false});cv.addEventListener('touchend',e=>{for(let t of e.changedTouches)delete TP[t.identifier];},{passive:false});cv.addEventListener('touchmove',e=>{e.preventDefault();const tl=[...e.changedTouches];if(Object.keys(TP).length<=1&&tl.length>=1){const t=tl[0];const pv=TP[t.identifier];if(pv){const dx=(t.clientX-pv.x)/R,dy=(t.clientY-pv.y)/R;Q=qNrm(qMul(qAx(0,1,0,-dx*2),qMul(qAx(1,0,0,dy*2),Q)));}TP[t.identifier]={x:t.clientX,y:t.clientY};}else if(tl.length>=2){const t1=tl[0],t2=tl[1],p1=TP[t1.identifier],p2=TP[t2.identifier];if(p1&&p2){const pa=Math.atan2(p1.y-p2.y,p1.x-p2.x),na=Math.atan2(t1.clientY-t2.clientY,t1.clientX-t2.clientX);Q=qNrm(qMul(qAx(0,0,1,na-pa),Q));const pd=Math.hypot(p1.x-p2.x,p1.y-p2.y),nd=Math.hypot(t1.clientX-t2.clientX,t1.clientY-t2.clientY);if(pd>5)ZM=Math.max(0.35,Math.min(5,ZM*nd/pd));}TP[t1.identifier]={x:t1.clientX,y:t1.clientY};TP[t2.identifier]={x:t2.clientX,y:t2.clientY};}},{passive:false});function loop(){FR++;updateFungi();if(AUTO)Q=qNrm(qMul(qAx(0,1,0,0.0022),qMul(qAx(0,0,1,0.00045),Q)));render();requestAnimationFrame(loop);}loop();